version: "2"

linters:
  default: standard
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused
    - misspell
    - revive
    - unconvert

formatters:
  enable:
    - gofmt
    - goimports

run:
  timeout: 3m
